# Sourced

A grounded answer engine: ask a question, get a streamed answer with inline citations and source cards, powered by the Brave Search API.

## Setup

> TODO: Full setup instructions (install, env, run, deploy) come in a later task.
